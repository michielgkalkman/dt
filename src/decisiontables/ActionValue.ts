import { ActionValues } from './ActionValues'

export class ActionValue {
    public actionValue: ActionValues

    constructor(actionValue: ActionValues) {
        this.actionValue = actionValue
    }
}